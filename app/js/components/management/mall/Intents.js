'use strict';

var React = require('react');

var Intents = React.createClass({

    render: function () {
        /* jshint ignore:start */
        return (
            <div className="Intents">
                Intents
            </div>
        );
        /* jshint ignore:end */
    }

});

module.exports = Intents;
